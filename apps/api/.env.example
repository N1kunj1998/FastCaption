# -----------------------------------------------------------------------------
# Option A: OpenAI (default when key is set)
# -----------------------------------------------------------------------------
# Get an API key at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# Option B: Ollama (free, local — only used when OPENAI_API_KEY is not set)
# -----------------------------------------------------------------------------
# 1. Install Ollama: https://ollama.com
# 2. Run a model: ollama run llama3.2
# 3. Set these and leave OPENAI_API_KEY unset to use Ollama instead:
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.2

# -----------------------------------------------------------------------------
# Auth (Apple / Google sign-in)
# -----------------------------------------------------------------------------
# Required for /api/auth/apple and /api/auth/google. Used to sign session JWTs.
JWT_SECRET=your-secret-at-least-32-chars

# Required for Google sign-in. Get from Google Cloud Console → APIs & Services → Credentials (OAuth 2.0 Client ID, iOS or Web).
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# -----------------------------------------------------------------------------
# MongoDB (optional — store user details on sign-in/sign-up)
# -----------------------------------------------------------------------------
# If set, users are upserted into database "fast-caption", collection "users".
# Get a connection string from MongoDB Atlas: https://www.mongodb.com/cloud/atlas
# MONGODB_URI=mongodb+srv://user:password@cluster0.xxx.mongodb.net/fast-caption?retryWrites=true&w=majority

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
PORT=3000
