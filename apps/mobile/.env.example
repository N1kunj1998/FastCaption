# Copy this file to .env and fill in your values.
# See README "Environment" section for details.

# -----------------------------------------------------------------------------
# API (optional — app works without it using mock data)
# -----------------------------------------------------------------------------
# Base URL of your backend. Used for script generation and remix on iOS/Android.
# If unset, the app uses mock script/remix responses.
# EXPO_PUBLIC_API_URL=https://your-api.example.com
# EXPO_PUBLIC_BASE_URL=https://your-api.example.com

# Force mock API (no network calls for generate/remix). Useful for UI-only development.
# EXPO_PUBLIC_USE_MOCK_API=true

# -----------------------------------------------------------------------------
# Settings links (optional — used in Settings tab)
# -----------------------------------------------------------------------------
# EXPO_PUBLIC_TERMS_URL=https://example.com/terms
# EXPO_PUBLIC_PRIVACY_URL=https://example.com/privacy
# EXPO_PUBLIC_PRO_URL=https://example.com/upgrade

# -----------------------------------------------------------------------------
# Auth (optional — only if you use the built-in sign-in modal)
# -----------------------------------------------------------------------------
# WebView auth (Cursor Create/Anything style): get from your auth provider.
# EXPO_PUBLIC_PROJECT_GROUP_ID — project/group ID from your auth provider
# EXPO_PUBLIC_HOST — your app's public host (e.g. your-app.example.com)
# EXPO_PUBLIC_PROXY_BASE_URL — auth proxy URL from your auth provider
EXPO_PUBLIC_PROJECT_GROUP_ID=your-project-group-id
EXPO_PUBLIC_HOST=your-app.example.com
EXPO_PUBLIC_PROXY_BASE_URL=https://proxy.example.com

# Sign in with Apple / Google (when EXPO_PUBLIC_PROXY_BASE_URL is not set):
# Apple works on iOS with no extra config (backend needs JWT_SECRET).
# Google: create an OAuth 2.0 Client ID (iOS/Android/Web) in Google Cloud Console,
# then set the same client ID here and as GOOGLE_CLIENT_ID in apps/api/.env.
# EXPO_PUBLIC_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# -----------------------------------------------------------------------------
# App URL (optional, for "Anything" / parent frame integration)
# -----------------------------------------------------------------------------
EXPO_PUBLIC_APP_URL=

# -----------------------------------------------------------------------------
# Upload & user content (optional, for upload features)
# -----------------------------------------------------------------------------
EXPO_PUBLIC_BASE_CREATE_USER_CONTENT_URL=https://user-content.example.com
EXPO_PUBLIC_UPLOADCARE_PUBLIC_KEY=

# -----------------------------------------------------------------------------
# Feature flags (optional)
# -----------------------------------------------------------------------------
# EXPO_PUBLIC_IS_ANYTHING_APP=false
# EXPO_PUBLIC_CREATE_ENV=production
