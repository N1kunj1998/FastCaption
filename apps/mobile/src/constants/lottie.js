/**
 * Lottie animation sources. Use with LottieView or EmptyState lottieSource.
 * EmptyState falls back to icon when Lottie fails to load.
 */
export const LOTTIE_EMPTY_STATE = require("../../assets/lottie/empty-state.json");
